setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis:hobby-dev
      as: REDIS

build:
  config:
    PORT:$PORT
  docker:
    web: ./frontend/Dockerfile
    worker: ./backend/Dockerfile

run:
  frontend:
    image: web
    command:
      - nginx -g 'daemon off;'
  backend:
    image: worker
    command:
      - node dist/index.js
